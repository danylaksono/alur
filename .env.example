# No environment variables are required.
# The AI copilot is bring-your-own-key: configure your OpenRouter API key
# in-app via the Settings dialog (stored in your browser's localStorage).

# Optional: replace the low-volume public Nominatim search endpoint with a
# compatible hosted or self-hosted endpoint for production traffic.
# VITE_GEOCODER_URL=https://nominatim.openstreetmap.org/search
